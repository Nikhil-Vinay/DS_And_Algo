/************************* Box Stacking Problem *********************************/
/**** There are n boxes each box has different length, width and height as (l1,w1,h1), (l2,w2,h2)....(ln,wn,hn).
 * Each bax has infinite number in quantity. We need to find out the maximum height can be formed in such a way
 * to place one box on another with the below set or rules:
 * 1. If box1 is placed on top of box2, box1 base length and width must be less than box2 top length and width.
 * 2. Box can be rotated in any direction.
 *
 * This question is asked in samsung.
 * This question is beautiful example of longest incresing subsequence dynamic problem *************/
